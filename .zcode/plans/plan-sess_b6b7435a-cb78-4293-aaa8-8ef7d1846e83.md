# 1Panel App 全量 UI 优化 + 全功能测试计划

**已确认边界**（来自你的回答）：
- **写入边界**：安全读全量 + 自建夹具写入（唯一前缀 `onepanel-e2e-*`、测完即清理）+ 容器操作（含重启/停止，**避开 3xui 和 Hysteria2**）
- **禁止修改**：面板端口(26314)/监听地址/安全入口/未认证设置/授权IP/域名绑定/面板 SSL——页面可查看、表单可校验，**禁止实际提交**
- **黑盒载体**：混合三层（API 层扩展 + 模拟器 integration_test + adb 走查）
- **UI 深度**：9 类问题清单修复 + 全模块截图走查

---

## 阶段 0：测试基建修复（先行，否则所有门禁不可信）
1. `test/scripts/test_runner.dart` 修复：① unit/ui/integration 失败时退出码非 0（当前永远 exit 0，门禁形同虚设）；② integration 档接入 `test/integration/` 9 个真服务器套件（当前只扫 test/api_client）；③ 保持 `.env` 门控（RUN_LIVE_API_TESTS）不变
2. 验证：故意注入一个失败测试确认退出码生效，再还原

## 阶段 1：UI 一致性修复（9 类问题清单，TDD 垂直切片：每类先写失败测试→修复→绿）
| # | 问题 | 位置 |
|---|---|---|
| 1 | AI 页死 leading（无汉堡/返回，shell 嵌入 Tab） | `lib/features/ai/ai_page.dart` |
| 2 | 容器文件页内部导航无返回（复用文件模块已验证的 inSubdirectory+PopScope 模式） | `lib/features/containers/pages/container_files_page.dart` |
| 3 | Scaffold 未显式 surface 背景（AGENTS.md 违规） | 热点：terminal/monitoring/dashboard/security/网站子页 |
| 4 | FAB 缺 heroTag ×2 | `macos_shell_content_page.dart:107`、`file_preview_page.dart:514` |
| 5 | 固定宽对话框 → AdaptiveLayoutSpec.dialogConstraints ×6 | openresty/compose_template/template_create/app_install/permission/path_selector |
| 6 | 硬编码文案 ~10 处 → l10n 中英文 key | file_preview、toolbox_clam、routing_rules、databases、dashboard（中文）等 |
| 7 | 桌面导航绕过 shell | `website_detail_page.dart` → openRouteRespectingShell |
| 8 | settings 手写 scaffold → ServerAwarePageScaffold；terminal 自造宽度检查 → AdaptiveLayoutSpec | 各自文件 |
| 9 | 3 处直接 showSnackBar → SnackBarUtils | grep 定位 |

每 3-4 类一批，批后跑 `flutter analyze` + `unit` + `ui` 门禁。

## 阶段 2：模拟器逐模块 UI 走查（已确认包含）
8 个 Tab 模块（服务器/文件/容器管理/应用/网站/AI/设置/安全）× 关键子页，adb 截图走查（手机宽为主、抽查平板/桌面宽三档），修复运行时发现的视觉/层级问题；overflow 守卫测试扩展到 dashboard 服务器卡、容器卡等高频卡片。

## 阶段 3：白盒测试补强（TDD 垂直切片，行为聚焦，不追行覆盖率）
按薄强度优先补 provider/service **行为测试**（状态机：加载/错误/空/重试）：
- 第一梯队：`ai`、`dashboard`、`containers`
- 第二梯队：`terminal`、`group`、`processes`
- 第三梯队：`security`、`onboarding`、`openresty`、`script_library`、`security_gateway`、`operations_center`、auth-feature
沿用 mocktail 边界模式与 `test/features/files/test_helpers.dart` 风格，测试名行为化。

## 阶段 4：API 层黑盒（扩展 test/integration/，直连真实服务器 154.9.228.190）
- 新增套件（复用 test_helper.dart + `.env` 门控）：
  - dashboard/monitor/logs 安全读全量
  - files `/tmp/onepanel-e2e` 夹具 CRUD（建→读→改→删）
  - cronjob/command/group 夹具 CRUD
  - database 测试库 + website 测试站（唯一前缀建删）
  - container 网络夹具 CRUD + 容器列表/详情/重启（按名称过滤跳过 3xui/Hysteria2）
- 危险面防护：面板基础配置接口仅验证只读路径可达，断言不发送修改请求
- 统一 fixture teardown（finally 保证清理、幂等删除），`RUN_LIVE_API_TESTS=true` 跑 integration 门禁

## 阶段 5：真机 UI 端到端（新建 `integration_test/`）
8-10 条核心旅程在模拟器（emulator-5556）自动驱动真实 App：添加服务器(US)→仪表盘数据→文件浏览/新建/删除→容器列表/详情/重启→网站列表→数据库→工具箱→设置只读校验。面板基础配置页只验证展示与表单校验，**不提交**。

## 阶段 6：回归与交付
`dart run test/scripts/test_runner.dart all` 全量回归 + 最终截图对比报告（每模块 Before/After）。

---

**约束**：不改 `docs/OpenSource/1Panel/**`；文件 ≤1000 LOC；遵守 AGENTS.md 分层与门禁；每阶段完成即门禁验证，不带失败推进。
**风险**：integration_test 依赖模拟器在线（已就绪）；真服务器夹具若清理失败会残留 `onepanel-e2e-*` 资源（teardown 用 finally + 幂等删除）；两套 android gradle 文件并存（Groovy 生效，applicationId=com.iskenkenya.onepanel）不动。