# 1Panel API 测试环境配置示例
# 复制此文件为 .env 并填写实际值
# 注意: .env 文件不应该提交到版本控制
#
# 1Panel API 使用 API密钥 + 时间戳 的认证方式
# Token = md5('1panel' + API-Key + UnixTimestamp)
# 需要在请求头中携带:
#   1Panel-Token: <md5_token>
#   1Panel-Timestamp: <unix_timestamp>

# ============================================
# 服务器配置
# ============================================
# 1Panel服务器地址 (例如: http://localhost:8080)
PANEL_BASE_URL=http://your-panel-server:port

# API版本
API_VERSION=v2

# ============================================
# 认证配置
# ============================================
# 1Panel API密钥 (从面板设置中获取)
# 获取方式: 登录1Panel面板 -> 设置 -> API接口 -> 生成API密钥
PANEL_API_KEY=your_api_key_here

# 接口密钥有效期（分钟），0表示不校验时间戳
TOKEN_VALIDITY_MINUTES=0

# ============================================
# 测试数据配置
# ============================================
# 测试用网站域名
TEST_DOMAIN=example.com

# 测试用IP地址
TEST_IP=127.0.0.1

# 测试用邮箱
TEST_EMAIL=test@example.com

# ============================================
# 功能开关
# ============================================
# 是否运行集成测试（需要真实服务器）
RUN_INTEGRATION_TESTS=false

# 是否运行破坏性测试（会修改数据）
RUN_DESTRUCTIVE_TESTS=false

# 测试超时时间（毫秒）
TEST_TIMEOUT=30000

# ============================================
# 日志配置
# ============================================
# 测试日志级别 (debug, info, warning, error)
TEST_LOG_LEVEL=info

# 是否保存测试日志到文件
SAVE_TEST_LOGS=true

# 测试日志文件路径
TEST_LOG_PATH=./test_logs
